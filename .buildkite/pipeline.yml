steps:
  - label: "Install Dependencies"
    command:
      - "npm install"
    agents:
      queue: "default"

  - label: "Build"
    command:
      - "npm run build"
    agents:
      queue: "default"
